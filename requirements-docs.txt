-r requirements.txt

sphinx>=7.0
# The linkify extra brings linkify-it-py, which docs/conf.py needs because
# it enables the linkify MyST extension.
myst-parser[linkify]>=2.0
furo>=2024.1.29
sphinx-copybutton>=0.5
